{{ ansible_managed | comment }}

# redirect dns request to jail with bind
rdr pass log on {{ host_net_ext_if }} proto {tcp,udp} \
  from any \
  to {{ host_net_ext_ip }} port 53 \
  -> {{ jail_net_ip }} port 53
